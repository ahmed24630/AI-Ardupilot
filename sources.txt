# sources.txt - one source or header file path per line, relative to this
# file's directory. Both .cpp and .hpp are accepted - headers won't be
# separately compiled, but listing them here makes them show up in IDEs
# like CLion or VS Code alongside their .cpp files.
#
# Lines starting with # are comments and are ignored.
# Blank lines are ignored.

src/main.cpp

# Add more as you build them, e.g.:
# src/board_detect.cpp
# src/board_detect.hpp
